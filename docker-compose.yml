services:
  swarmclaw:
    image: ghcr.io/swarmclawai/swarmclaw:latest
    build: .
    ports:
      - "3456:3456"
      - "3457:3457"
    volumes:
      - ./data:/app/data
    env_file:
      - path: ./.env.local
        required: false
    restart: unless-stopped
