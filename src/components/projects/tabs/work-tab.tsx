'use client'
import type { Mission, Project } from '@/types'
export function WorkTab({ project, missions }: { project: Project; missions: Mission[] }) {
  return <div className="p-8 text-text-3/50 text-[13px]">Work tab — coming soon</div>
}
