'use client'
import type { Mission, Project } from '@/types'
export function OverviewTab({ project, missions }: { project: Project; missions: Mission[] }) {
  return <div className="p-8 text-text-3/50 text-[13px]">Overview tab — coming soon</div>
}
